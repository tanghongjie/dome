<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/statusbar_bg</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="android:windowBackground">@color/main_bg</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
    <style name="AppStartLoad" parent="AppTheme">
    </style>
    <!--加载进度框样式-->
    <style name="pb_dialog_style" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
    <!--加载弹窗动画样式-->
    <style name="loading_dialog_style">
        <item name="android:windowEnterAnimation">@anim/loading_dialog_enter</item>
        <item name="android:windowExitAnimation">@anim/loading_dialog_exit</item>
    </style>
    <!-- 自定义对话框  的动画效果 -->
    <style name="Animation.InputMethod" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/input_method_enter</item>
        <item name="android:windowExitAnimation">@anim/input_method_exit</item>
    </style>
    <style name="common_loaddialog_style" parent="android:Theme.Dialog">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@style/Animation.InputMethod</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>
    <!--界面顶部view使用的样式:用于适配沉浸式状态栏-->
    <style name="topview_style">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:id">@id/title_top_view_id</item>
    </style>
    <!-- 通用的返回箭头的样式ImageButton -->
    <style name="back_icon_style" parent="back_icon_base_style">
        <item name="android:src">@drawable/ic_back_white</item>
        <item name="android:background">@drawable/selector_maincolor_8dp</item>
    </style>

    <style name="back_icon_base_style">
        <item name="android:gravity">center</item>
        <item name="android:padding">12dp</item>
        <item name="android:scaleType">centerInside</item>
        <item name="android:layout_width">@dimen/common_title_height</item>
        <item name="android:layout_height">@dimen/common_title_height</item>
        <item name="android:id">@id/title_btn_back_id</item>
    </style>
</resources>
